layers:
  - defaults/dependent
  - platforms/libvirt
builder:
  iso_url: "${var.build_directory}/${var.source}/libvirt/${var.source}"
  iso_checksum: none
  disk_image: true
  # use_backing_file: true
  skip_resize_disk: true
